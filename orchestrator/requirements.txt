# LLM providers
anthropic>=0.40.0
openai>=1.30.0          # OpenAI + Google Gemini (via OpenAI-compatible endpoint)

# FastAPI API layer
fastapi>=0.115.0
uvicorn>=0.34.0
sse-starlette>=2.1.0
pydantic>=2.0.0
python-dotenv>=1.0.0

# Auth
PyJWT>=2.8.0
httpx>=0.27.0

# Database
psycopg2-binary>=2.9.0

# Streamlit UI (login screen)
streamlit>=1.35.0
watchdog>=6.0.0
streamlit-cookies-controller>=0.0.4

# Sub-agent / data-processing dependencies (subprocess, same interpreter)
paho-mqtt>=2.1.0
pandas>=2.0.0
numpy>=1.26.0
scipy>=1.12.0
matplotlib>=3.8.0
